This package contains classes that are defined to accept,save and return datas to the front end whenever requested.
The Database class is designed to assume the responsibility of a database,even though its just an arraylist class.
The person class defines a person Object.