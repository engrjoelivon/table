The package contains just the connector class,its sole purpose is to connect the frontend to the backend.Going by the software achitecture
rule,the backend should never directly connect to the front end.
It contains methods used to access the database and also create instances of ad hoc person.